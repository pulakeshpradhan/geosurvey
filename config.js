/* GeoSurvey — team defaults baked into this deployment (edit on GitHub, no build step).
 * endpoint: your Apps Script Web App URL (…/exec). When it is set here, enumerators open the app and
 * everything is connected — nothing to paste. Leave it empty to let each phone enter the URL in Settings.
 * A phone can also be set up by opening  <app URL>?db=<Web app URL>  (Settings → "Copy team link"). */
const GS_CONFIG = {
  endpoint: '',
};
